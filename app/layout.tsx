import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "WebP Converter - Free Online Image Converter",
  description:
    "Convert your images to WebP format instantly in your browser. No uploads, no servers - 100% client-side conversion with quality and size controls.",
  keywords: [
    "webp converter",
    "image converter",
    "png to webp",
    "jpg to webp",
    "online converter",
    "free image converter",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
